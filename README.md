# 多附表 Excel 时长自动累加工具

根据姓名自动匹配，支持多个附表累加到主表，自动清空 0 值。

## 功能
- 选择主表
- 添加多个附表
- 每个附表可选择累加到不同列
- 自动清空所有为 0 的单元格
- 不修改原文件，导出新表

## 使用方法
1. 运行 main.py
2. 选择主表（必须包含姓名列）
3. 添加多个附表（必须包含姓名、时长列）
4. 选择每个附表要累加的列
5. 点击开始处理，保存结果